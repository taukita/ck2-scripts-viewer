<Window x:Class="Ck2ScriptsViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		
		xmlns:treeModel="clr-namespace:Ck2ScriptsParser.TreeModel;assembly=Ck2ScriptsParser"
		
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		mc:Ignorable="d" 
        d:DataContext="{d:DesignInstance Type=treeModel:Node}"
		
        Title="MainWindow" Height="600" Width="800">
	
	<Window.Resources>
		<HierarchicalDataTemplate DataType="{x:Type treeModel:Node}" ItemsSource="{Binding Path=Children}">
			<TextBlock Text="{Binding Path=FullName}"/>
		</HierarchicalDataTemplate>
	</Window.Resources>
	
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Margin="5" Padding="5" Click="ButtonBase_OnClick">Загрузить</Button>
        </StackPanel>
        <TreeView Margin="5,0,5,5" Name="ScriptView" ItemsSource="{Binding Path=Children}" Grid.Row="1"/>
    </Grid>
</Window>
